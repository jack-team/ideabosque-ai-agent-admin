  <script type="text/javascript" src="{{sdkUrl}}"></script> 
  <div id="chat-bot-window"></div>
                      
  <script type="text/javascript">  
    document.addEventListener('DOMContentLoaded', function() {
      // Window Mode
      const chat = AiChatSdk.createChat({
        openMode: 'window',
        target: "#chat-bot-window",
        position: "{{position}}"
        clientId: "{{clientId}}",
        configs: {
          userId: "{{userId}}",
          endpointId: "{{endpointId}}",
          agent: "{{agent}}",
          agentName: "{{agentName}}",
          coordination: "{{coordination}}"    
        }
      });
      chat.init();
    });
  </script>