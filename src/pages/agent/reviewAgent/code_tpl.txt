  <script type="text/javascript" src="{{sdkUrl}}"></script> 
  <div id="chat-bot-{{openMode}}"></div>  
                    
  <script type="text/javascript">  
    document.addEventListener('DOMContentLoaded', function() {
      // {{openMode}} Mode
      const chat = AiChatSdk.createChat({
        openMode: "{{openMode}}",
        target: "#chat-bot-{{openMode}}",
        position: "{{position}}"
        clientId: "{{clientId}}",
        configs: {
          userId: "{{userId}}",
          endpointId: "{{endpointId}}",
          agent: "{{agent}}",
          agentName: "{{agentName}}",
          coordination: "{{coordination}}"    
        }
      });
      chat.init();
    });
  </script>